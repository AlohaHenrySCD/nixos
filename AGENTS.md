# 用户授权规则

- 默认允许执行 `nix eval` 求值操作，无需逐次请求许可。
- 在用户明确许可前，不得写入文件，包括创建、修改、删除文件，以及命令产生的间接写入。
- `nix eval` 的默认许可仅涵盖求值，不包含文件写入；应使用 `--no-write-lock-file` 等适用选项避免隐式写入。需要写入时，必须先获得用户明确许可。
- 用户明确要求写入某个文件即视为对该次指定写入的许可，不扩展为其他文件写入的许可。
